## 概要

本轮尝试在 /workspace 内按“启动服务 → 浏览器巡检 → TDD 修复”流程执行自动调试，但仓库当前仅包含 README.md，未发现 apps/web 或任何可运行的前端/后端代码，因此无法启动本地 web 服务与执行链路巡检。

## 已完成改动

- 清理了 Git 远端配置中的内嵌凭证，改为不含凭证的 HTTPS 地址（避免凭证泄露风险）。

## 验收结论

- 无法对 apps/web 进行巡检与修复：目标目录不存在且仓库树仅包含 README.md。

## 下一步所需输入

- 提供包含 apps/web 的项目源码（直接同步到 /workspace，或提供可拉取的仓库与所需访问方式）。
- 若认证页/摊主端/主办方端需要登录，请提供测试账号（或说明如何在本地使用 mock/跳过鉴权）与必要的 .env 示例项（不要提供真实密钥）。
