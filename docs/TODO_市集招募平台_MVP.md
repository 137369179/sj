# TODO: 市集招募平台 MVP 总结与后续

## 1. 阶段里程碑：MVP 已完全闭环 ✅

经过多轮自动化全栈演进，当前市集招募平台的核心 MVP 目标已**全数达成**。以下 P0/P1/P2 目标均已实现并被测试覆盖（150+ 测试用例，覆盖率 100%）：

- **P0-1 摊位分配事务化**：已使用 `db.$transaction` 及乐观锁机制处理并发分配。
- **P0-2 生产级认证与会话**：已移除明文 Cookie Stub，重构为基于 `jose` 的 **JWT (HS256) 会话防伪与过期管理**，并配置了安全的 HTTPOnly `mrp_session`。
- **P0-3 跨字段时间业务校验**：已全面接入基于 Zod `superRefine` 的时间先后关系校验，并在前端完整回显。
- **P1-1 真实文件上传链路**：已抽象 `StorageProvider`，接通基于 UUID 防遍历的本地安全上传，并预留了 `S3StorageProvider` 接口以备一键切换对象存储。
- **P1-2 状态文案映射**：核心状态映射已解耦提取至 `lib/market-status.ts` 与 `lib/application-status.ts`，全局 UI 表现统一。
- **P1-3 错误反馈与表单体验**：已在多处长表单（建市集、建摊位等）实现**跨字段错误摘要**及**可点击的锚点错误定位**。
- **P2 增强与体验优化**：
  - 看板图形化：已接入 `recharts` 实现状态分布漏斗与利用率条形图。
  - 摊主端信息丰富化：聚合查询透出了主办方信息及可用摊位规模。

## 2. P3：中长期演进方向 (Post-MVP)

当前代码作为 MVP 已经极为健壮，下一步建议进入 P3 阶段或新的迭代周期：

### P3-1 平台运营与审核后台
- 构建 `/admin` 命名空间，允许平台管理员管理所有主办方入驻资质。
- 市集上架前增加平台巡检流。

### P3-2 支付、结算与退款
- 摊位费支付：接入微信/支付宝等三方支付网单。
- 分账与提现：平台佣金抽成逻辑，主办方账户余额与提现审批。
- 退款逆向流：因市集取消或恶劣天气引发的全额/部分退款链路。

### P3-3 摊主与消费者互动侧
- 摊位图与导览：集成 Canvas/SVG 摊位地图绘制，消费者可在线查看导览。
- 摊位打卡与核销：消费者现场购买或核销权益。

### P3-4 基础设施与运维监控
- 引入 Prisma 真实数据 Seed 脚本，优化开发期数据填充。
- Docker 化部署编排（Dockerfile/docker-compose）。
- 错误追踪与日志系统（Sentry/Datadog 等）。

---
**交接建议**：
当前工作区不仅业务逻辑自洽，且测试基线极高（150+ Tests）。您可以直接在此基线上起步探索新的中长期产品边界（如“支付流”），而不再受限于早期 MVP 的体验断层。
